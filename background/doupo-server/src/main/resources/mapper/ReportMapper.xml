<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.doupo.mapper.ReportMapper">

    <select id="getUsersByTime" resultType="java.lang.Integer">
        select count(id) from user
        <where>
            <if test="begin != null">
                and create_time &gt;= #{begin}
            </if>
            <if test="end != null">
                and create_time &lt;= #{end}
            </if>
        </where>
    </select>

    <select id="getOrdersByTime" resultType="java.lang.Integer">
        select count(id) from orders
        <where>
            <if test="begin != null">
                and order_time &gt; #{begin}
            </if>
            <if test="end != null">
                and order_time &lt; #{end}
            </if>
            <if test="status != null">
                and status = #{status}
            </if>
        </where>
    </select>

    <select id="getSalesTop10" resultType="com.doupo.dto.GoodsSalesDTO">
        select od.name, sum(od.number) as number from order_detail od, orders o
        where od.order_id = o.id
        and o.status = 5
        <if test="begin != null">
            and o.order_time &gt;= #{begin}
        </if>
        <if test="end != null">
            and o.order_time &lt;= #{end}
        </if>
        group by od.name
        order by number desc
        limit 0, 10
    </select>

    <select id="getOrderStatistics" resultType="com.doupo.dto.OrderStatisticsDTO">
        SELECT
        DATE(order_time) as orderDate,
        COUNT(id) as totalCount,
        SUM(IF(status = 5, 1, 0)) as validCount
        FROM orders
        <where>
            <if test="begin != null">
                and order_time &gt;= #{begin}
            </if>
            <if test="end != null">
                and order_time &lt;= #{end}
            </if>
        </where>
        GROUP BY DATE(order_time)
        ORDER BY orderDate ASC
    </select>

    <select id="getTotalUserCountBefore" resultType="integer">
        select count(id) from user where create_time &lt; #{time}
    </select>

    <select id="getNewUserCountList" resultType="com.doupo.dto.UserStatisticsDTO">
        select date(create_time) as regDate, count(id) as count
        from user
        where create_time between #{begin} and #{end}
        group by date(create_time)
    </select>

</mapper>
